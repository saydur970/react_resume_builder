import { Grid } from '@mui/material';

export const Preview = () => {
  return (
    <Grid item xs={12} container
      sx={{backgroundColor: '#fff', height: '60rem', margin: '5rem 0'}}
    >
      preview

    </Grid>
  )
};